# /test

Run the project test suite.

## Instructions

1. Run `lib/bashunit tests` from the project root
2. If any tests fail, read the failing test file and corresponding source file
3. Report a summary: total tests, passed, failed
4. For failures, show the test name, expected vs actual values, and the source file location
