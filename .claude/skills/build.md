# /build

Build the single-file executable.

## Instructions

1. Run `./build.sh` from the project root
2. Verify `bin/create-pr` was generated and is executable
3. Verify `bin/checksum` was generated
4. Report the file sizes and the SHA256 checksum
